//Record Gif
if(keyboard_check_pressed(ord("G"))) {
	//uncomment to be able to record gifs
	//gifrecord = !gifrecord;
	if(gifrecord) {
		//gif = gif_open(gui_width, gui_height);
	} else {
		//Save Gif
		//gif_save(gif, "capture.gif");
	}
}

if(gifrecord) {
	//gif_add_surface(gif, application_surface, 2, 0, 0, 2);
}